# SecureShield API
A Flask-based API demonstrating JWT authentication and role-based access control (RBAC).

## Features
- **JWT Authentication:** Secure user login and token generation.
- **Role-Based Access:** Restricted admin routes (RBAC).
- **Security Logging:** All unauthorized attempts are logged in `security.log`.
- **Integrity Checks:** Validation of token signatures to prevent tampering.
